{% extends 'QuartetKakarotBundle::base.html.twig' %}

{% block headline %}部署　編集{% endblock headline %}

{% block content_row %}
    <div class="container">
        <div class="contents">
            {{ form(edit_form) }}
            <br>
            <a href="{{ path('division_show', { 'id': entity.id }) }}" class="btn btn-danger">削除</a>
            <br>
            <br>
            <a href="{{ path('division') }}" class="btn btn-warning" role="button">一覧に戻る</a>
        </div>
    </div>
{% endblock content_row %}